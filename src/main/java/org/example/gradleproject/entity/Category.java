package org.example.gradleproject.entity;



public enum Category {
    COMPUTER_SCIENCE,
    PHILOSOPHY,
    RELIGION,
    SOCIAL_SCIENCES,
    LANGUAGE,
    SCIENCE,
    TECHNOLOGY,
    ARTS_RECREATION,
    LITERATURE,
    HISTORY_GEOGRAPHY
}

/*
public enum Category {
    CATEGORY1,
    CATEGORY2,
    CATEGORY3,


    AUTHOR_SERVICE(new AuthorService()),
    BOOK_SERVICE(new BookService());

    private BaseService service;

    Category(BookService bookService) {
    }

    public BaseService getService() {
        return service;
    }

    public void setService(BaseService service) {
        this.service = service;
    }
}
*/